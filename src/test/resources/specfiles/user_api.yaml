openapi: 3.0.3
info:
  title: Example API
  version: 1.0.0
paths:
  /users/{userId}:
    get:
      summary: Get user by ID
      parameters:
        - $ref: './components/parameters/userId.yaml'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: './components/schemas/User.yaml'
        '404':
          $ref: './components/responses/userNotFound.yaml'
components: {}
